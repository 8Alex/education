let arr = ['js', 'css', 'html'];

alert(arr[0]);

let array = [0, 1, false, 2, undefined, '', 3, null];
let result = array.filter(Number);

alert(result);

const indexArray = [
    [1, 2],
    [1, 2, 3],
    [1, 2, 3, 4]
]
const res = arr.findIndex(el => el.length > 3);
alert(res);